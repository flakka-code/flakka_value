// ignore_for_file: prefer_const_constructors
import 'package:flakka_value/flakka_value.dart';
import 'package:test/test.dart';

void main() {
  group('FlakkaValue', () {
    test('can be instantiated', () {
      expect(FlakkaValue(), isNotNull);
    });
  });
}
